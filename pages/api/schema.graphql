
type Note {
  id: Int!
  text: String!
  author: String!
  createdAt: String!
  updatedAt: String!
}

type List {
  id: Int!
  name: String!
  notes: [Note!]!
}

type Query {
  allNotes: [Note!]!
  allLists: [List!]!
  getList(id: Int!): List
}