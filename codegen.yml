overwrite: true
schema: "http://localhost:4000" # replace with your server URL
documents: "./**/*.graphql"
generates:
  ./types/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
