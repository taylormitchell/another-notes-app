export type Note = {
  id: string;
  content: string;
  created_at: string; // assuming each note has a created_at field
};
export type List = {
  id: string;
  name: string;
  created_at: string;
};
