{
  "name": "another-notes-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "bash scripts/prod.sh --build",
    "start": "bash scripts/prod.sh --build --run",
    "deploy": "bash scripts/prod.sh --build --deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
